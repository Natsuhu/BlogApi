<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.natsu.blog.mapper.MusicCatalogMapper">

    <!-- 获取歌单列表 -->
    <select id="getMusicCatalogList" resultType="com.natsu.blog.model.dto.MusicCatalogDTO">
        select id, name, description, is_published, create_time, update_time
        from music_catalog
        <where>
            <if test="queryCond.keyword != null and queryCond.keyword != ''">
                name like concat('%',#{queryCond.keyword},'%')
            </if>
        </where>
        order by create_time asc
    </select>

</mapper>
